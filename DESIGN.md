---
name: SHiNe MUSIC
description: Android 原体验在 NAS Web 上的响应式延续
colors:
  brand-seed: "#ED5564"
  dark-background: "#111113"
  dark-surface: "#1B1B1F"
  dark-container: "#25252B"
  light-background: "#F8F7FA"
  light-foreground: "#202124"
  success: "#2E7D5B"
  danger: "#BA1A1A"
typography:
  family: "system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Noto Sans SC', sans-serif"
  headline-small: "24px/32px 700"
  title-large: "22px/28px 500"
  title-medium: "16px/24px 500"
  body-medium: "14px/20px 400"
  label-medium: "12px/16px 500"
spacing:
  1: "4px"
  2: "8px"
  3: "12px"
  4: "16px"
  6: "24px"
  8: "32px"
radii:
  artwork: "3px"
  control: "8px"
  container: "12px"
  dialog: "28px"
  pill: "999px"
---

# SHiNe MUSIC Design Contract

## 设计来源

手机 Web 的视觉与行为以 Android/Compose 和 16 张现有截图为金标准。Web 实现应翻译 Compose 的结构与状态，而不是依据当前 Web 页面重新设计。

规范来源：

- `app/src/main/kotlin/com/metrolist/music/`：导航、页面、播放器、菜单、分析与手势。
- `fastlane/metadata/android/en-US/images/screenshots/screenshot_1.jpg` 至 `screenshot_16.jpg`：视觉回归基线。
- `app/src/main/kotlin/com/metrolist/music/ui/theme/Theme.kt`：Material 3 tonal、深浅色与纯黑逻辑。
- `app/src/main/kotlin/com/metrolist/music/constants/Dimensions.kt`：关键尺寸与动效节奏。

Sendspin 只新增同步状态和房间交互。它不得改变首页、搜索、媒体库、播放器、队列、歌词、分析和推荐的既有表达。

## 设计性格

熟悉、沉浸、克制。界面以封面和正在播放内容建立氛围，以 Material 3 tonal 表面建立层级，以清晰状态支持家庭共享。

避免管理面板式卡片堆叠、玻璃拟态、霓虹装饰、渐变文字和无意义入场动画。圆角、阴影和主色均按功能使用，不做统一装饰。

## 响应式结构

### Compact：小于 768px

- 顶部应用栏 64px，底部四主导航 80px。
- 迷你播放器 64px，位于导航上方并保留 8px 间距。
- 页面单列滚动；横向媒体模块保留横滑，不改成纵向长列表。
- 完整播放器使用可收起底部页，竖向第二页显示相似本地音乐。

### Medium：768–1199px

- 横屏或平板可将底部导航改为窄侧轨。
- 内容增加列数；媒体详情可使用双栏，但保持手机信息顺序。
- 队列、歌词、相似和分析以抽屉或可折叠面板呈现。

### Wide：1200px 及以上

- 左侧导航建议 224–256px；中央内容自适应；右侧上下文建议 320–384px。
- 右侧上下文在队列、歌词、相似音乐和分析之间切换，可折叠为零宽。
- 底部播放器常驻，高度 80–96px；展开态使用三部分工作区。
- 高密度曲库行可为 48–56px；分析行仍需容纳 chips、状态和雷达。

断点只重排容器。路由、查询、队列、播放状态、菜单动作和服务端数据不得分叉成手机与 PC 两套实现。

## 颜色系统

### Material 3 tonal

默认种子色为 SHiNe Coral `#ED5564`。由种子色生成 primary、secondary、tertiary、surface、container、outline 和 error 等语义角色，不直接散布固定色值。

浅色、深色和纯黑主题共享同一语义角色。纯黑只将背景和主要 surface 设为黑色，仍用 tonal container、描边和前景色表达层级。

活动导航、主播放按钮和播放进度使用 primary。活动曲目优先使用 secondaryContainer；错误、危险删除和收藏红心分别使用 error 与明确图标语义。

### 动态封面配色

完整播放器从当前封面提取一至两个主色，生成 tonal 渐变或模糊背景。封面切换时渐变约 800ms 交叉淡化，模糊图层可使用更慢的约 2s 淡化。

动态背景必须叠加对比度保护层。所有文字、图标和焦点轮廓在最终合成背景上满足 AA；提取失败时回退当前主题色，不显示突兀默认渐变。

封面、歌词和控制是前景，动态色只提供环境氛围。不得把彩色背景扩散到所有内容页面。

## 字体与图标

使用系统无衬线字体，匹配 Compose 的默认字体行为。中英文标题、曲目元数据和控制标签不得依赖未随包发布的字体。

- 页面或媒体标题：24/32，必要时 700。
- 应用栏标题：22/28，500。
- 曲名与主要操作：16/24，500。
- 元数据：14/20，400。
- 状态、时间与 chips：12/16，500。

图标优先复用 Android drawable 的语义与轮廓。品牌位置使用 SHiNe 实际图标，不使用音符字符占位。

## 尺寸与形状

关键手机尺寸直接继承 Android 基线：

- 应用栏：64px。
- 底部导航：80px；紧凑模式可为 64px。
- 迷你播放器：64px；与导航间距 8px。
- 队列收起工具栏：64px。
- 普通曲目行：64px；本地分析曲目行：84px。
- 搜索来源区：48px；搜索输入框：44px。
- 列表封面：48px；常用网格封面：128px。
- 播放器水平内边距：32px。

所有点击或触控目标至少 44×44px。可视图标可以更小，但其命中区域不得缩小。

封面通常使用 3px 圆角；本地分析封面可使用 6px。工具控件使用 8px，内容容器最多 12px，对话框可使用 28px，胶囊只用于明确的成组控制或状态。

## 应用外壳

### 手机导航

底部只保留首页、搜索、媒体库、本地音乐四个主入口。选中态同时改变容器、图标和标签，不只依赖颜色。

重按当前入口滚动到顶部；切换入口保存并恢复该入口的导航栈和滚动位置。主入口切换按索引方向移动约八分之一屏宽，并在 200ms 内淡入淡出。

顶栏显示页面标题、历史、统计和设置。同步房间通过首页快速访问、播放器状态和设置进入，不新增一个挤压原导航的主标签。

### PC 导航

左侧导航承载四个主入口、同步房间和设置。同步房间与设置属于辅助区，与四个音乐主入口保持视觉分组。

中央内容继续使用手机端页面的模块与组件语义。右侧上下文属于播放器扩展，不得变成永久固定的单一队列栏。

## 首页

首页是可重排的信息流，不是一组等宽统计卡。

- Speed Dial 使用自适应分页网格，至少三列；首屏末位为 Randomize，底部显示页码点。
- Quick Picks 使用带吸附的横向行列；单项包含封面、标题、元数据和更多菜单。
- 继续聆听可使用圆形艺人或方形媒体封面，保持 Android 截图中的疏密关系。
- 每日发现、社区、账号歌单、遗忘收藏、相似推荐、情绪风格和国内推荐保持各自卡型。
- 页面支持下拉刷新、加载占位和渐进加载；当前播放项显示封面覆盖态。

PC 增加可见列数和横向空间，不把这些模块改写成统一卡片或数据表。

## 搜索

搜索页顶部由返回键、44px 搜索框和清除按钮组成。其下是两组 22px 圆角双联容器，组间 8px，共四个图标来源。

活动来源使用容器内平移的 primary 胶囊，图标切换到 onPrimary。图标需有“国内歌曲、国内歌单、曲库、高级”可访问名称。

空查询显示可换行的历史 chips。查询结果使用原歌曲、歌单、艺人卡型，并保留当前播放覆盖态、长按菜单和下载动作。

PC 保留同一个来源切换器。宽屏时可将输入和结果置于中央，将高级筛选置于左侧或右侧，但不得改名或改变筛选语义。

## 媒体库与媒体详情

媒体库顶区依次包含排序、结果数量、搜索和列表/网格切换。智能歌单排在用户歌单之前，创建 FAB 在手机始终可达。

歌单详情以大封面为 hero。无封面显示音乐图标，单封面显示完整图片，多封面显示四宫格；可编辑歌单提供封面编辑覆盖按钮。

刷新、随机、更多和主播放按钮叠在封面右下角。主播放按钮 64px，使用 primary；其余按钮 48px，使用高对比 surface。

标题居中，下面显示曲目数与总时长。曲目行保留 active container、播放覆盖态、更多菜单、多选和可编辑歌单重排。

专辑页保留居中封面、标题、可点击歌手、年份、曲目数、时长、收藏、播放和更多。艺人页保留全宽渐隐 hero 与透明到实色的应用栏。

PC 将 hero 与元数据并排，并为列表提供列标题、排序、键盘多选和右键菜单；手机的信息与动作不能因此消失。

## 本地音乐与分析行

待分析和已分析使用独立分组标题。待分析可折叠；已分析默认展开。

84px 分析行结构：56px 封面、可伸缩元数据列、64px 分析区。标题单行，第二行显示歌手与专辑，第三行放置紧凑 chips。

chips 显示“120 BPM”、学术调性、Camelot 编号、分析中、分析失败或 `MISS` 字段。长文本省略但保留完整可访问说明。

分析未完成时，右侧显示带环形进度的状态按钮；完成后显示七维雷达。当前曲目使用 secondaryContainer，并在封面上显示播放或暂停图标。

大列表使用窗口化渲染。窗口化不得破坏焦点顺序、屏幕阅读器总数、活动曲目定位或多选状态。

## 高级分析控件

### 模糊滑块

BPM 和七维参数使用目标值加容差。轨道高亮目标左右的有效范围；拖动自动启用该条件，“任意”可显式禁用。

每个滑块提供数值输入、增减按钮和键盘方向键操作。屏幕阅读器读出维度名、目标、容差和启用状态。

### Camelot 轮盘

轮盘内圈为 A 小调，外圈为 B 大调，分为 1–12。选中扇区与容差邻区使用不同 tonal 层级；中心按钮切换“任意”。

轮盘仅在手势起点位于扇区时接管拖动，并识别垂直滚动意图。键盘替代使用方向键换编号、A/B 键换模式、加减键改容差。

### 七维雷达

轴顺序固定为愉悦度、能量、律动、原声、器乐、现场感、人声。不同页面不得改变轴顺序。

拖动仅在命中控制点后生效；长按切换该维度启用状态。容差以控制点外的低对比光晕表达，同时提供文本数值，不能只靠图形。

结果行高 84px，右侧使用 64px 对比雷达；中部显示相似度、BPM 差值、学术调性与 Camelot 差值。

## 播放器

### 迷你播放器

手机迷你播放器为 64px 高浮动胶囊，圆角 32px，可选 outline。背景支持默认、透明、模糊、渐变和纯黑，但内容对比度必须稳定。

左侧为带播放进度的封面或播放按钮，中间为曲名与歌手，右侧放置收藏等快捷动作。整条左右滑切歌，并用弹簧动画回到中心。

### 完整播放器

顶部显示“正在播放”与队列或来源标题。中部在封面和歌词间切换；封面保持前后曲目的水平 pager。

曲名与歌手可滚动显示，点击进入媒体详情，长按提供复制。收藏与分享使用成组胶囊；歌词态改为全屏与歌词菜单。

进度区域显示当前时间、总时长以及居中的来源、编码或码率 chip。主控制为上一首、68px 播放或暂停、下一首。

底部工具栏顺序固定为队列、相似续播、随机、歌词、循环、更多。工具栏也是展开队列的收起态，不创建重复控制。

封面单击切换分析雷达，长按开始分析。左右双击每次快退或快进 5 秒，连续双击在短时间内递增倍数。

暂停时封面可缩至约 0.97。切歌、背景和按钮形状可使用弹簧或交叉淡化，不加入装饰性漂浮动画。

### 歌词

同步歌词以当前行建立最强层级，非当前行降低不透明度。自动滚动将当前行平滑置中，用户滚动时暂停跟随并给出恢复提示。

点击同步歌词跳转播放位置；长按进入多选，可复制或分享。无歌词、加载中和获取失败分别显示骨架、空状态和重试入口。

### 相似音乐页

手机通过播放器竖向第二页进入。顶部保留透明迷你播放器，标题为“相似本地音乐”，副标题说明按 BPM、调性和 7 维情绪向量匹配。

列表显示相似度、BPM、调性和小雷达。选择结果后播放推荐队列并回到第一页；向下拖动第二页顶部可以收起播放器。

PC 将同一内容放入右侧“相似”标签。完整播放器展开时，封面或歌词、控制、相似或队列形成三部分结构。

## 队列与菜单

队列展开态显示队列标题、数量和总时长。锁定状态禁止重排与滑删；解锁后支持拖动排序、滑动移除、长按多选与曲目菜单。

歌曲菜单至少覆盖收藏、加歌单、分享、下一首播放、加入队列、查看歌手或专辑、置顶快速访问、分析、下载和适用的移除动作。

手机菜单使用底部页，PC 使用上下文菜单或 popover。两种呈现调用同一动作模型，并在危险操作前显示明确目标和确认。

## Sendspin 房间表达

房间页面展示房间名称、成员数量、创建、加入、离开和删除。加入按钮必须写明“加入并启用声音”。

播放器附近只显示连接状态：未加入、连接中、同步中、重连中、异常。可展示只读的自动校准状态，但不得把服务器时钟偏移标成听感“同步误差”。

主房间页面不放动态手工补偿滑块。自动时钟、缓冲和漂移修正由 Sendspin 持续管理。

手动设置的准确名称为“高级：外部输出固定延迟校准”。说明文字为：“仅校正蓝牙、声卡或音响 DSP 的稳定链路延迟；Sendspin 自动同步仍会持续运行。”

高级静态校准按设备保存，显示毫秒值、重置按钮和生效输出说明。无法识别输出设备时标记为当前浏览器配置，不暗示网页已选择系统音频设备。

## 动效与手势

- 主入口：方向性八分之一屏宽滑移 + 200ms 淡入淡出。
- 顶栏：显示约 300ms、隐藏约 200ms 的淡化。
- 播放器：无弹跳或低弹跳 spring；导航随展开进度滑出。
- 迷你播放器：拖动跟手，越界阻尼，释放后切歌或弹簧回中。
- 活动封面：播放覆盖态即时；背景颜色使用交叉淡化。
- 相似续播图标：可按当前 BPM 轻微脉冲，但不得影响可读性。

在 `prefers-reduced-motion: reduce` 下，取消滑移、脉冲、封面缩放和长交叉淡化。保留不超过 100ms 的状态切换，避免界面失去反馈。

## 键鼠与可访问性

- 所有交互可使用键盘完成，焦点顺序与视觉顺序一致。
- 使用 `:focus-visible` 的双层高对比轮廓，不移除浏览器默认焦点而无替代。
- 播放、收藏、下载、循环、随机、同步和分析状态都有可访问名称与按下状态。
- 进度、音量、BPM 和容差使用原生 slider 语义，并提供格式化数值。
- 雷达与轮盘提供等价的表单或键盘模式；Canvas 本身不能成为唯一交互界面。
- 分析进度、下载结果、房间重连和同步异常通过礼貌级 live region 通知。
- 对比度按 WCAG 2.2 AA；颜色、位置和动效都不能成为唯一信息载体。
- 自动播放失败时保留明确按钮；重连和刷新不得抢夺键盘焦点。

## 视觉回归验收

16 张 Android 截图全部纳入手机 Web 视觉回归。允许变化的内容仅包括动态数据、封面、系统字体栅格和浏览器安全区。

必须稳定比较：导航位置、模块顺序、播放器层级、封面比例、按钮尺寸、圆角、间距、字体层级、主题角色、分析图形和主要交互状态。

至少覆盖常见手机、平板、1366px 和 1920px。PC 用同一页面状态验证左中右结构与底部播放器，不能以另一套静态样稿代替。

## 禁止退化

- 不得以当前 Web 的简单快速卡、通用曲目表和基础播放器作为完成标准。
- 不得把 Sendspin 的连接界面当作完整播放器体验。
- 不得删除四主入口、原首页模块、歌词、队列、分析或相似续播来换取响应式简化。
- 不得使用字符音符替代 SHiNe 品牌图标。
- 不得让手机与 PC 的组件状态、动作名称或服务端语义各自演化。
