# SHiNe MUSIC Product Contract

## 产品定义

SHiNe MUSIC 是部署在家庭 NAS 上的免登录音乐服务。NAS 统一保存曲库、分析数据、收藏、歌单、历史与下载任务，浏览器负责浏览、播放、整理和多设备同步控制。

访客连接局域网并打开服务地址后即可使用。手机 Web 取代 Android APK；Android 源码保留为功能、视觉与交互基准，不再进入发布流程。

曲库可来自多个 NAS 路径、USB 设备、网络挂载或云盘挂载。每个音频库可独立启停、扫描、筛选和设置只读状态；设备离线时保留索引。

## 产品金标准

手机 Web 必须复现现有 Android/Compose 产品体验，不以当前 Web 页面作为设计基线。

以下资料共同构成手机端视觉与行为金标准：

- `app/src/main/kotlin/com/metrolist/music/` 下的 Compose 页面、播放器与交互实现。
- `fastlane/metadata/android/en-US/images/screenshots/` 下 `screenshot_1.jpg` 至 `screenshot_16.jpg` 的全部截图。
- 截图定义可见结果，Compose 定义状态、手势、导航、菜单和边界行为。

发生冲突时，先保留用户可见的原有行为，再针对浏览器平台限制做等价适配。不得用新建的通用音乐面板替换已有产品表达。

截图分组用于视觉回归：

- 1–3：首页弹层、在线内容与关于页。
- 4：搜索入口、来源切换与历史。
- 5–7：封面播放器、歌词播放器与完整控制。
- 8–10：歌单详情、媒体库与首页信息流。
- 11–13：BPM、Camelot 与 7 维高级搜索。
- 14–16：相似续播、分析雷达与分析进度。

## 产品原则

- 原体验优先：迁移既有界面与交互，不重新发明手机端。
- 播放始终可达：任何主页面都保留当前播放与核心控制。
- 同一业务模型：手机与 PC 共用路由、状态、组件语义和服务端接口。
- 音乐优先：封面、曲名、播放状态、歌词、队列和分析结果拥有最高层级。
- 状态可解释：扫描、分析、下载、离线、同步和重连都有明确反馈与恢复入口。
- 局域网共享：收藏、歌单、历史和下载写入 NAS；独立播放状态保留在浏览器。

## 手机信息架构

手机底部导航固定为四个主入口：

1. 首页
2. 搜索
3. 媒体库
4. 本地音乐

同步房间是新增功能入口，不替代四个主入口。它从首页快速访问、播放器同步状态和设置进入；加入房间后，状态持续显示在播放器附近。

主页面顶栏保留当前标题以及历史、统计、设置入口。重复点击当前底部导航项应回到页面顶部；切换主入口应恢复各自的页面栈和滚动位置。

## 首页验收

- 保留 Speed Dial 分页网格、页码指示和首屏末位“随机播放”。
- 保留 Quick Picks、每日发现、继续聆听、账号歌单、遗忘收藏和社区内容。
- 保留相似推荐、情绪与风格、国内音乐推荐及场景入口；无数据时隐藏对应模块。
- 卡片点击执行播放或导航；长按打开类型对应菜单，并提供可感知反馈。
- 当前播放项目显示播放或暂停覆盖态，不只改变文字颜色。
- 支持下拉刷新、渐进加载、重按“首页”回顶和加载占位。
- 同步房间只作为快速访问卡或状态入口加入，不挤掉原首页模块。

## 搜索验收

- 顶部使用返回键与 44px 高圆角搜索框，输入态提供清除按钮。
- 搜索框下方保留两组双联胶囊，共四个图标来源：国内歌曲、国内歌单、曲库、高级。
- 选中项使用可移动的主色胶囊；来源选择在刷新或重开页面后保持。
- 空查询显示搜索历史 chips；提交结果写入共享历史，暂停历史时不写入。
- 国内歌曲支持直接播放与下载入库；国内歌单可进入详情并导入共享歌单。
- 曲库搜索覆盖曲名、歌手、专辑和文件名，并保留歌曲、歌手、歌单筛选。
- 语音识别仅在浏览器能力和安全上下文允许时启用；不可用时说明原因，不隐藏搜索主流程。

## 媒体库验收

- 默认显示已点赞、已缓存和“我的 Top N”等智能歌单，再显示用户共享歌单。
- 支持名称、创建时间、曲目数和最近更新排序，并可切换升降序。
- 支持即时搜索、列表/网格切换、结果数量和空状态。
- 创建按钮常驻可达，提供新建歌单与导入网络歌单。
- 歌单详情保留大封面或四宫格封面、标题、曲目数、总时长和活动曲目状态。
- 封面区域保留刷新、随机、更多和主播放操作；共享歌单支持多选、排序与重排。
- 歌曲、歌单、专辑和艺人均有上下文菜单；手机使用底部菜单，不能只剩单个删除按钮。

## 本地音乐验收

- 未配置音频库时显示说明与“去储存设置”，不显示无意义空表格。
- 顶部支持搜索、数量和按扫描时间、名称、歌手、BPM、调性、能量排序。
- 曲目分为“待分析”和“已分析”；待分析分组可折叠并显示数量。
- 本地曲目行高 84px，包含封面、标题、元数据、分析 chips 和 64px 分析区域。
- chips 至少显示 BPM、学术调性、Camelot 编号和缺失字段；分析区域显示进度、重试或 7 维雷达。
- 当前曲目使用容器色和封面播放覆盖态；点击播放，长按打开完整歌曲菜单。
- 支持随机播放当前筛选结果；大曲库必须使用服务端分页和列表虚拟化。

## 播放器验收

- 迷你播放器是悬浮在底部导航之上的 64px 胶囊，显示封面进度、曲名、歌手和快捷操作。
- 左右滑动迷你播放器切歌；点击展开同一播放器状态，不创建第二套播放实例。
- 完整播放器以底部页展开，返回或下滑收起；导航随展开进度退场。
- 竖屏第一页包含封面或歌词、元数据、收藏、进度、音源信息、上一首、播放、下一首和工具栏。
- 竖屏第二页为“相似本地音乐”；横屏和 PC 将同一内容重排，不删除功能。
- 封面可左右滑动切歌；单击切换分析雷达，长按启动分析，左右双击执行递增式快退或快进。
- 工具栏保留队列、相似续播、随机、歌词、循环和更多；状态以图标形态与文字语义共同表达。
- 歌词支持自动居中、点击跳转、长按多选、复制或分享、搜索、编辑和偏移设置。
- 队列支持标题、数量、总时长、锁定编辑、多选、拖动排序、滑动移除和上下文菜单。

## 音频分析验收

NAS 负责分析，Web 负责展示与控制。分析结果必须持久化，并可被曲库、搜索、播放器和推荐共同消费。

每首已完成分析的音乐包含：

- BPM。
- 学术调性与 Camelot 调性。
- 愉悦度、能量、律动、原声、器乐、现场感、人声七个归一化维度。

分析任务包含等待、运行、完成、失败状态和可读进度。失败可重试；已有结果可重新分析；缺少字段必须明确显示。

## 高级搜索验收

- BPM 范围为 40–220，并提供目标值与容差。
- Camelot 轮盘支持 1A–12A 与 1B–12B，中心可切回“任意”，并显示相邻容差。
- 七个情绪维度同时支持滑块和雷达两种输入方式，并共享统一容差。
- 筛选值分为草稿与已应用状态；用户明确执行后才更新结果。
- 雷达只在手势起点命中控制点时接管拖动；其余手势保留给页面滚动。
- 结果按相似度排序，显示相似百分比、BPM 差值、调性差值和对比雷达。
- 无筛选、分析数据不足和无匹配结果使用不同的空状态与行动建议。

## 相似续播验收

相似续播用于保持听感风格连续，而不是生成静态推荐列表。

- 当前曲目必须具备完整七维向量和有效 BPM；缺失时解释原因并提供分析入口。
- 候选曲 BPM 与当前曲相差不超过 5，调性满足同调、平行大小调或 Camelot 左右邻调。
- 候选曲按七维欧氏距离换算的相似度降序排列。
- 默认排除近期 12 首，保留最多 10 首已播历史，并预先准备下一首。
- 更新推荐邻居不得重置当前曲目、播放位置或播放状态。
- 上一首沿推荐历史返回；下一首优先使用已预备曲目，避免切歌等待。
- 手机第二播放器页显示结果；PC 右侧面板显示同一结果和分析依据。
- 房间模式由 NAS 生成一次推荐队列并共享；Sendspin 只同步最终播放，不参与推荐计算。

## Sendspin 同步房间

Sendspin 是新增的播放同步底座，不是 SHiNe 前端、播放器或产品交互的替代品。

SHiNe 继续负责房间创建、命名、删除、成员状态、队列、播放命令和界面；Sendspin 负责时钟估算、预缓冲、统一起播和漂移校正。

- 任意访客可创建、加入、离开和删除房间。
- 每台设备必须主动点击“加入并启用声音”，以满足浏览器自动播放限制。
- 主界面显示未加入、连接中、同步中、重连中或异常，不把 NAS 时钟偏移误写为“同步误差”。
- 自动同步与漂移修正由 Sendspin 持续执行，不提供误导性的动态手工滑块。
- 手动值仅出现在“高级：外部输出固定延迟校准”中，用于蓝牙、声卡或音响 DSP 的稳定链路延迟。
- 高级静态校准需说明它位于 Sendspin 自动同步之后，不替代自动校准；支持重置并按设备持久化。
- 重连恢复房间与暂停位置，但浏览器或 NAS 重启后不得自动发声。

## PC Web 验收

PC 端是同一业务模型和设计语言的结构性放大，不是独立产品，也不是运维面板。

- 左侧为主导航与同步入口，中间承载首页、搜索、媒体详情和高密度曲库。
- 右侧上下文面板在队列、歌词、相似音乐和分析之间切换，并可折叠。
- 底部播放器常驻，复用同一播放状态、队列、模式、同步状态和媒体会话。
- 展开播放器时，以 Android 横屏双栏为基础扩展为封面或歌词、元数据与控制、上下文面板三部分。
- 首页模块、搜索来源、歌单 hero、分析雷达和相似结果保留原语义，只随空间增加列数和信息密度。
- 曲库支持键盘、右键或更多菜单、多选、排序、批量播放和虚拟列表。
- 手机与 PC 不维护两份业务页面；断点只改变布局容器与操作呈现方式。

## 主题、动效与可访问性

- 使用 Material 3 tonal 色彩逻辑，支持浅色、深色和纯黑主题。
- 默认品牌种子色为 `#ED5564`；播放器可从当前封面提取主色与渐变，并保证前景对比度。
- 手机交互目标不小于 44px；关键播放器尺寸遵循 Android 金标准。
- 主入口切换使用约八分之一屏宽位移与 200ms 淡入淡出；播放器和滑动操作使用克制弹簧。
- `prefers-reduced-motion` 下取消位移、脉冲和非必要视差，保留即时状态反馈。
- 以 WCAG 2.2 AA 验收键盘、焦点顺序、语义标签、对比度和屏幕阅读器提示。
- 颜色不得成为唯一状态信号；雷达、轮盘和手势必须有键盘与文本替代操作。

## 首版边界

- HTTP 局域网入口按普通 Web 验收；安装型 PWA、Service Worker 和输出设备选择待受信任 HTTPS 后验收。
- Android 专属的 MediaStore、Android Auto、系统均衡器和系统触觉不直接迁移，需提供 Web 等价反馈或能力说明。
- 不迁移旧 Android 数据库，但必须迁移其产品体验、分析语义和推荐行为。
- 同步目标是家庭听感一致，不承诺专业多房间系统的样本级精度。

## 完成定义

- 16 张参考截图对应的核心状态均有手机 Web 视觉回归用例。
- 首页、搜索、媒体库、本地音乐、播放器、高级搜索和相似续播通过逐项验收。
- 三台设备可加入同一房间；同步控制不改变独立播放、推荐或原播放器交互。
- 常见手机、平板、1366px 和 1920px 视口使用同一业务状态完成完整播放流程。
- NAS 扫描、分析、收藏、歌单、历史与房间在容器重启后保持，离线音频库不丢索引。
